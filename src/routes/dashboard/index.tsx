import { component$ } from '@builder.io/qwik';

export default component$(() => {

    return (
        <>
            <h3>Admin Dashboard</h3>
            <p>Está ruta debe de ser privada</p>
        </>
    )
});